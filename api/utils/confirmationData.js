var strings = require("./strings")
var formatDate = require("../utils/formatDate")
//unsued

module.exports = {
    getProviderIntakeSubmitted(values) {
        let data = [
            ``,
        ];
        return data;
    }
}